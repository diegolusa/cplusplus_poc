#include <iostream>
#include <map>
using namespace std;


int main()
{
    int tam_vet;
    map<int, int> contagem();

    cout << "Informe a quantidade de valores: ";
    cin >> tam_vet;
    
    int valores[tam_vet];
   
    for (int i = 0; i < tam_vet; i++)
    {
        cout << "Posição[" << i << "]: ";
        cin >> valores[i];
        //contagem.find(valores[i])
        if ()
    }

    

}